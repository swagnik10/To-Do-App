docker build for k8s -> docker build --build-arg ENV_FILE=.env.k8s -t swagnik/todo-frontend:1.1 .
docer build for docker -> docker build --build-arg ENV_FILE=.env.docker -t swagnik/todo-frontend:1.1 .


minikube has been installed in local machine -> C:\Users\hp\.minikube

minikube

choco install minikube -y

after install to check the version -> minikube version

change the context -> docker context use desktop-linux

install kubectl -> choco install kubernetes-cli -y

to get the kubectl version -> kubectl version --client

to start minikube -> minikube start --driver=docker

kubectl get node
kubectl get node -o wide

to check the addons -> minikube addons list
metrics server -> minikube addons enable metrics-server
add the dashboard -> minikube addons enable dashboard
to get the dahboard -> minikube dashboard


to check the minikube status -> minikube status
to delete -> minikube delete

to start and stop the MINIKUBE
minikube stop
minikube start

