First build and then run the docker compose file

docker compose up --build

Docker compose run 

when we have environment files to store the passwords

docker compose --env-file docker-compose.env up -d  				-> for run if we did any changes in docker files		
docker compose --env-file docker-compose.env up -d --build			-> for build if we did any code changes

connect sql in iteractive mode

docker exec -it todo-sqlserver bash

connect db

/opt/mssql-tools18/bin/sqlcmd \
-S localhost \
-U sa \
-P 'YourStrongPassword123!' \
-C

/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P 'YourStrongPassword123!' -C

now run the queries 

first query
then go


check logs 

docker logs <container_id>



SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_SCHEMA = 'Todos' AND TABLE_NAME = 'TodoItem';


SELECT DB_NAME() AS DatabaseName, t.name AS TableName FROM sys.tables t WHERE t.name = 'TodoItem';

if you change the sql objects then only restart the backend service will enough

docker compose restart backend



to stop & start the docker compose containers

docker compose stop
docker compose start


Rebuild and restart after code/config changes

docker compose down   -> this will delete the containers
docker compose up --build -d

to check the status 

docker compose ps

to view logs

docker compose logs


to delete database volumes This deletes your database data.

docker compose down -v

to check the volumes
docker volume ls

to check the connection string in interactive mode of backend

docker exec -it todo-backend printenv

to check after apply the environment  values

docker compose --env-file docker-compose.env config

